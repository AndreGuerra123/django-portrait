============
Installation
============

At the command line::

    $ easy_install django-portrait

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-portrait
    $ pip install django-portrait
